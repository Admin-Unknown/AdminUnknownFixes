---------------------------------------------------------------------------------------------------
Version: 2.1.28
Date: 2026-08-12
  Bugfixes:
    - Fix the load failure on melamine-resin, reported as "fluid with name 'saps' does not exist". This mod adds saps to that recipe and had been calling it a fluid. It is an item in pyalienlife and always has been, so the name was real, the recipe was real, and the two only fell out at the end of the load, in a message naming neither the mod nor the line that did it
    - The recipe belongs to pyhightech and saps to pyalienlife, and having the one says nothing about having the other, so the addition now asks for saps to be there as well as the recipe
    - Where an entry names something real but calls it the wrong kind, and the name is plainly the other kind, it is now read as the other kind at the end of the load and said so in the log. That can only be told apart from a thing a later mod was going to add once every mod has finished adding, which is why it happens there and not with the rest of the recipe tidying
  Modding:
    - check-prototype-names.ps1 knew saps was real and let it through. The base game is not in othermodsource, so a name any upstream recipe asks for counts as proof the name exists, and that proof was being read as though it settled the kind as well. It does not: every upstream mention of saps is an item, and this one asked for a fluid. Upstream mentions now carry the kind, and only a mention of the same kind will do
    - Being put in a hand or a chest says the name is an item, since nothing else can be, so those mentions carry a kind too

---------------------------------------------------------------------------------------------------
Version: 2.1.27
Date: 2026-08-12
  Bugfixes:
    - angelsaddons-storage still stopped the load with "table index is nil" after 2.1.26, because putting the recipes into shape only helps if it happens before somebody walks them, and 2.1.26 guessed at when that would be. It walks them in data-updates, and this mod loads after it, so nothing done in that stage arrives in time
    - Angel's keeps that walk in a field of a table it leaves in the global state, and all sixteen places that start one read the field at the moment they call. So the pass now sits in front of it: whoever starts a walk, in whatever stage, the ingredients and results are put into the form the game takes first, and then the walk runs exactly as it always did
    - That is the whole of the load order problem gone rather than worked around. There is nothing to keep in step as mods are turned on and off, and the extend guard is no longer needed for this failure, only for the bobpower one it was written for

---------------------------------------------------------------------------------------------------
Version: 2.1.26
Date: 2026-08-12
  Bugfixes:
    - Fix the load failure in angelsaddons-storage, reported as "table index is nil" in angelsrefining's override-functions.lua. Angel's walks every recipe in the game early in data-updates and reads each ingredient and each result as a table with a name in it. It found one without, took the nothing it got back, and tried to use it as a key
    - Factorio 2.0 stopped taking the shorthand pair that 1.1 allowed for ingredients and results, and every mod has been through its own files for that, but it still turns up in what mods write to each other's recipes while loading, where the game does not look at any of it until long after Angel's has walked it
    - So every ingredient and every result is now put into the form the game itself asks for, before anybody walks them: the shorthand written out in full, a name given on its own written out in full, an entry that never says whether it is an item or a fluid read as whichever it turns out to be, and anything naming nothing at all taken out, since there is no telling what it was meant to be. Each one is named in the log with the recipe it came from
    - The pass needs to run before Angel's looks, and this mod loads after Angel's, so the extend guard calls it at the top of data-updates. That is the last moment before the walk. Without the guard the pass still runs, but only in time for the stages after the one that breaks, so 0-auf-extend-guard 0.0.3 is worth having
    - The one place this mod still wrote the 1.1 shorthand itself was the More Science Packs file, where all forty-two additions were in it. Those never reached Angel's: pypostprocessing reads the name out first, finds nothing, and stops the load trying to say so, which is what anyone running that mod would have got
    - That file also cleared each lab input in place, leaving a hole in the middle of the list, and a list with a hole in it is only half a list to everything that reads it afterwards. What is kept is gathered into a new one now

---------------------------------------------------------------------------------------------------
Version: 2.1.25
Date: 2026-08-12
  Bugfixes:
    - Fix the load failure on jetpack-1, reported as "item with name 'py-science-pack-1' does not exist". Py's four science packs all come from pyalienlife and nowhere else, but a mod that wants to cost its research in them checks for whichever py mod it happens to know about and then names one. Jetpack does this, which is why turning pypetroleumhandling on stopped the load in a mod that has nothing to do with it
    - Every technology is now looked over at the end of the load and anything it is researched with that nothing installed provides is taken out, rather than naming the technologies this happens to. The same goes for the packs a lab will accept, which fail the same way
    - A technology left costing nothing would be researched the instant it was clicked, so the last pack is replaced with automation science rather than removed, keeping the count it asked for. Every technology changed either way is named in the log, so the ones that matter can be given a considered cost
  Bugfixes:
    - Fix the load failure on rf-rare-earth-mine, reported by reverse-factory as "item with name 'bob-red-inserter' does not exist". The recipe it was reversing is py's rare earth mine, which this mod had told to ask for a red inserter, and the name of that is not the name of an item
    - With Bob's inserter overhaul on there is a red inserter between the yellow and the fast one, but it is only ever an entity: Bob's builds it out of the long handed inserter, which keeps its own name and gains the red icon. So bob-red-inserter cannot go in a recipe at all, and the mine now asks for long-handed-inserter
    - The name was picked in the 2.1 rename sweep because it exists upstream, which it does, as the wrong sort of thing. So every name this mod puts in a recipe has now been checked against what kind of prototype it actually is, and eight more were wrong
    - Four of those would have stopped the load the same way, each behind a mod combination not yet tried: angels-gas-oxygen written as gas-oxygen in the rocket oxidizer capsule, angels-stone-crushed as stone-crushed in the hpf stone recipe, bob-calcium-chloride as calcium-chloride in the sodium carbonate recipe, and bob-player-brain-2 and bob-player-head-2 as their unprefixed names in the immunosupressants recipe
    - brake-03 in the exoskeleton recipe was never a name at all: py's are brake-mk01 through mk04, and mk03 is the one that belongs beside the other mk03 parts already in there
    - energy-shield-mk3-equipment is bob-energy-shield-mk3-equipment now, and it comes from bobequipment while everything around it comes from bobclasses, so it is asked for separately and only when bobequipment is installed
    - Four more were doing nothing rather than stopping the load, because taking an ingredient out of a recipe that has not got it is quietly allowed: gas-oxygen again in the Angel's steel plate recipe, basic-circuit-board for bob-basic-circuit-board in the air filter, rtg for bob-rtg in the player power core, and angels-dueterium-fuel-cell, which is angels-deuterium-fuel-cell spelled wrong and so has never once matched
  Modding:
    - check-prototype-names.ps1 has an ingredient mode. The other modes ask whether a name exists; this one asks what it is, because an ingredient has to be an item or a fluid and an entity of the same name will not do. The base game is not in othermodsource, so a name any upstream mod puts in a recipe, or in somebody's hands, counts as proof
    - It was reading ingredients as prototype definitions, which meant every name used in a recipe vouched for itself and the check could never have failed. An amount is what tells the two apart
    - It was also reading the worked example inside the block comment in functions.lua as though it were code, and now skips block comments
    - The two ingredient patterns were written as a two element array, and a comma binds tighter than a plus, so they were concatenated into one and asking for the first of them handed back its first character. It matched every letter t in every mod: seven million of them, and nothing useful
    - Skipping the lines that cannot match before running a dozen regexes over them takes the whole run from six minutes to thirty seconds
    - check-optional-deps.ps1 now reads bobmods.plates and its like as a guard. Each Bob mod raises a flag as it loads, and this mod tests those in a few places. Only the flags one mod alone can raise are read: logistics is raised by boblogistics and by bobinserters, so it says nothing about which is there
---------------------------------------------------------------------------------------------------
Version: 2.1.23
Date: 2026-08-12
  Bugfixes:
    - 0-auf-extend-guard 0.0.2, which needs downloading separately like the last one. Two things in 0.0.1 could make it do nothing at all without saying so, and either would leave the bobpower error looking exactly as it did before the guard existed
    - It stood aside if anything already answered for the data table, on the grounds that combining the two was guesswork. It now goes underneath instead, answering for data:extend and passing every other key straight through to whatever was there before
    - It also gave up if data:extend was not a field it could read directly, which would be so if the game ever handed it out through a metatable of its own. It now looks the ordinary way as well, and only gives up if there is genuinely no function there, which it writes to the log
    - It now checks that it actually took the field and writes either AUF: the extend guard is in place or AUF: the extend guard did not take the field to the log, naming what answers instead. 0.0.1 installed without a word, which left no way to tell a guard that was working from one that was not there at all
    - A second line at the end of the data stage says whether the guard was still answering once every mod's data.lua had been and gone, and how many wrappers were laid over it. If something takes it out of the chain partway through, that line is what will say so
  Info:
    - Having the zip in the mods folder is not the same as the mod being ticked in the mod list, and enabling it needs a restart. The README says so now, along with how to check the log
---------------------------------------------------------------------------------------------------
Version: 2.1.22
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on urea-gasification, which asked for an icon it had never been given. A recipe shows the icon of what it makes, and with more than one product it shows whichever main_product names. This one makes gas urea and ash and names neither, so there was nothing to show
    - It could not name one. main_product would have to be the gas, and pypostprocessing looks up whatever a recipe names as an item, which the gas is not. So the recipe now carries Angel's gas urea icon of its own, read from the fluid rather than named as a file, which keeps it right if they redraw it
    - The recipe has been like this since it was written and only became a problem now, because it is only built when Angel's gas urea and compressed air both exist, and turning on the Py high tech pack is what brought those about
    - Any recipe left in that state at the end of the load is now given the icon of its first product. This is worth doing generally rather than for the one recipe, because adding a result to somebody else's single-product recipe takes its icon away, and this mod adds results to other mods' recipes throughout. Each one is named in the log, so an icon that looks wrong can be given a considered one
  Modding:
    - Checked the 28 recipes this mod writes for the same shape. urea-gasification was the only one
---------------------------------------------------------------------------------------------------
Version: 2.1.21
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on sand-processing. Factorio 2.1 changed the mine-entity research trigger: where it named one entity it now takes a list of them, any one of which finishes the research. bobplates still writes the old form, and the Angel's fix in here rewrites which entity it names without changing the shape, so we hand it on just as broken
    - Every mine-entity trigger is now brought to the new shape at the end of the load rather than that one being named. This is the same kind of change as recipe categories becoming a list, and it reports the same way, against the technology rather than the line that wrote it
    - The trigger's entities are also checked against what is installed, and a name nothing provides is dropped so long as something is left to mine. If nothing is, that is written to the log and left alone, since which entity should take its place is a decision rather than a guess. The sand-processing case has such a decision already: Angel's Refining removes bob-quartz, and the fix beside it names Angel's quartz instead, or falls back to crafting sand
    - That fallback was counting with the wrong word. A craft-item trigger reads count and nothing reads amount, so it was being ignored and the default of one used. The intent was one, so nothing changes in play
    - Checked every research trigger the upstream sources write while here. Only bobplates uses mine-entity; the rest are craft-item and unaffected
---------------------------------------------------------------------------------------------------
Version: 2.1.20
Date: 2026-08-11
  Bugfixes:
    - The repair for bobpower released in 2.1.19 could not work from here, and this release ships it as a second mod, 0-auf-extend-guard, which needs installing alongside this one. bobpower's data stage runs before ours, so the tolerant data:extend was arriving after the mod it was meant to catch
    - We are later in the load than the name suggests. Requiring pycoalprocessing holds this mod behind everything that sorts before pycoalprocessing does, which is every Bob mod. Nothing about our own name changes that, and the requirement cannot go: every override in here is written to run after the mods it overrides
    - The new mod requires nothing at all and its name begins with a zero, so it runs before any mod has had the chance to hand anything to data:extend. Loading that early means pypostprocessing wraps data:extend after it rather than before, and a plain wrapper would sit underneath and never see the list, so it answers for the field itself instead: reads get its own version, writes are kept as what that version should call. Whoever wraps it later ends up inside it
    - It is only needed for a mod that leaves a stray field in the list it hands to data:extend, and bobpower 3.0.0 is the only one in the sources checked. It is harmless otherwise
    - The copy of this that lives in AdminUnknownFixes stays for mods that load after us, and now stands aside when the new mod is present. Both at once would be one too many: ours would read the field, find the guard, and put itself between the guard and pypostprocessing, whose metatables would then stop being attached
  Modding:
    - Both packaging scripts now build every companion mod from a list of folders rather than naming the one, and copy each zip into stubs/ for direct download. The README documents what each companion is for and why it cannot live in the main mod
---------------------------------------------------------------------------------------------------
Version: 2.1.19
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on turning bobpower on, reported against pypostprocessing's metas.lua from inside bobpower's own fluid generator file. The hydrazine generator item there closes one line early, leaving its weight sitting in the list handed to data:extend rather than in the item it belongs to
    - Factorio reads the numbered entries of that list and ignores anything else, so bobpower is fine on its own and has probably been like this for a while. pypostprocessing replaces data:extend with one that walks every key to attach its metatables, and reads that stray weight as though it were a prototype
    - Neither mod can see the other and the file cannot be edited from here, so data:extend now passes on only the entries the game itself would have used, and says in the log what it left behind. Any mod loading after us with the same slip is covered, and the snapshot of upstream sources was checked for others: bobpower is the only one
    - This one depends on load order in a way worth writing down. It works because nothing sorts this mod after an unrelated one, so our data stage runs before bobpower's. Adding bobpower to our dependencies would move us behind it and quietly undo the fix
---------------------------------------------------------------------------------------------------
Version: 2.1.18
Date: 2026-08-11
  Bugfixes:
    - Fix the loop in the technology tree between angels-advanced-chemistry-5, angels-nitrogen-processing-4, rocket-fuel, kovarex-enrichment-process and utility-science-pack. Angel's has advanced chemistry 5 cost utility science packs and nitrogen processing 4 wait for advanced chemistry 5, while the road to the utility pack runs back through nitrogen processing 4, so the two of them close a circle
    - The line that breaks it has been here all along, sat behind checks for pyalienlife and pypetroleumhandling that belong to the py science pack swap on the line below it. Nothing about the loop depends on which py mods are installed, so the removal now happens wherever angelspetrochem does
    - It also drops the utility pack from what nitrogen processing 4 costs, which matters for the same reason the prerequisite did: the pack is unlocked by a technology that waits on nitrogen processing 4, so charging that technology in the pack it leads to would be a lock in its own right even with the tree untangled
  Modding:
    - The technology tree is walked at the end of the load, and a loop found there is written out in full, naming every technology in it and which prerequisite was dropped to break it. The game's own message names the technologies but not what put them in that order, which is the part that costs the afternoon
    - Breaking it means the game still starts, on one prerequisite fewer. The cut falls on whichever link closes the circle rather than on whichever link deserves to go, so a loop reported in the log is still worth a considered fix here. Run against this release's loop it happens to pick the same link by itself
---------------------------------------------------------------------------------------------------
Version: 2.1.17
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on the lab. Py replaces the vanilla lab with a five by five one and Bob's chains that lab to its own three by three bob-lab-2, and an upgrade is only allowed between two buildings of the same size. Neither mod is wrong on its own and neither can see the other, so the pair only fails once both are installed
    - Bob's burner lab chains the other way into the same lab, so that link was the next one to fail. Both are now dropped rather than the one that happened to be reported
    - Every upgrade link is checked at the end of the load, against all of what the game asks of one: the two buildings must be the same size, collide with the same things and share a fast replaceable group, and the target must be minable, not flagged as unupgradable, and built by an item that is still visible. That last one is how the item replacer can break a link it never touched, which is what the chemical plant and refinery fix has been working around by name
    - A link that cannot stand is removed rather than either building being resized, since a building whose footprint no longer matches its graphics is worse than an upgrade planner that has forgotten one step. Nothing already built changes, and each removal says in the log which building and which condition it was
---------------------------------------------------------------------------------------------------
Version: 2.1.16
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on bob-heat-shield. The technology was being given py-science-pack-3 to research with from behind checks for bobrevamp and angelssmelting, but the pack and the technology named after it are both pyalienlife's. A science pack ends up in the technology's unit ingredients, so a missing one is reported against the technology rather than the line that added it
    - The same pack is added to five angelsindustries rocket technologies, which needed the same check
    - The helper that rewrites a technology to cost one py science pack now returns early if that pack does not exist, which covers all eight technologies it is used on
    - Prerequisites naming a technology from a mod that is not installed fail the same way, and four were doing it: the acetylene technology is pyfusionenergy's rather than pyrawores', the seaweed one pyalienlife's rather than angelsbioprocessing's, and the energy shield one bobequipment's rather than bobclasses'
    - The alien artifact recipes are unlocked by bobtech's alien research, so that mod is now part of what the file is loaded for alongside bobenemies and pyalienlife
  Modding:
    - check-optional-deps.ps1 reads science packs, lab inputs and technology prerequisites and unlocks. A pack is not an ingredient in the sense the script understood, and a prerequisite is just a name in a list until the technology is validated, so none of these were being looked at. Adding them turned up the four prerequisite bugs above before they could each cost a load
    - It also reads ingredients written as a name and a count rather than a typed table, which is the form a technology unit takes
    - It understands a function that bails out at the top rather than wrapping its body, and it leaves Angel's recipe builder fallback lists alone, since those are lists of alternatives where a name being absent is the point
---------------------------------------------------------------------------------------------------
Version: 2.1.15
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on alien-from-poor. The four alien artifact recipes are crafted in a genlab out of pyalienlife's samples, serum and blood, but they were only behind a check for bobenemies, which supplies the artifacts and nothing else. Without pyalienlife the genlab crafting category does not exist and the first of the four recipes takes the load down with it
    - The two recipes in that file that also need pyalternativeenergy for fatty acids and pyfusionenergy for super alloy now check for those mods in turn
    - Guard the rest of the overrides that reach into a mod the surrounding condition does not imply: ceramic in the silicon nitride recipe is pyhightech's under a bobplates check, graphite and saps in the heat shield tile are pyhightech's and pyalienlife's under a bobrevamp check, small parts are pypetroleumhandling's under pyrawores, pyfusionenergy and angelsindustries checks, carbolic oil is pypetroleumhandling's under pyhardmode, and kerosene is pyrawores' rather than pypetroleumhandling's
    - The two ores that are switched to mining with a fluid now check that the basic-with-fluid resource category exists first. It is pyalienlife's, and without it the ore was being told to require a fluid its category cannot support
    - The seaweed to coal gas recipe checks for seaweed itself, since angelsbioprocessing having the algae says nothing about pyalienlife being installed
  Modding:
    - check-optional-deps.ps1 now treats a mod check as proof of that mod's own hard dependencies, which is what most of these guards were quietly relying on: a block behind pyalternativeenergy may use pyalienlife's prototypes freely because the one cannot load without the other. That alone accounted for more than half of what it had been reporting
    - It reads categories and subgroups as well, because a recipe can be correct on every ingredient and still fail on the building it is crafted in. That is the shape of this release's crash, and it found a second one the same day
    - It skips files nothing requires, rather than reporting problems in code the game never reaches, and it follows the settings and control entry points instead of only the data stage ones
    - It reads conditions that run over several lines, so an existence check written under the if it belongs to now counts
---------------------------------------------------------------------------------------------------
Version: 2.1.14
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on coalgas-from-seaweed. Angel's prefixed its item subgroups as well as its recipe categories, so the recipe belongs in angels-bio-processing-green. This is the same shape of failure as the category one in 2.1.13: the field is only resolved when prototypes are validated, so the recipe is blamed rather than the line that set it
    - bobrevamp renamed its RTG technology and recipe to bob-rtg, which left the sodium carbonate recipe unlocking a technology that does not exist and the two prerequisite edits beside it doing nothing
    - bobelectronics renamed advanced-electronics-3 to bob-advanced-processing-unit and kept the old name only on the icon file, so the pyhightech branch was re-parenting and deleting a technology that was no longer there
    - The clarifier crafting speed now checks that the furnace-to-assembling-machine conversion above it actually ran, rather than assuming a machine it may not have created
  Modding:
    - check-prototype-names.ps1 now validates every field that names another prototype rather than just recipe categories: subgroup, group, fuel, resource, equipment and ammo categories, and damage types. A subgroup is what broke this release and nothing was looking at one. Where a field is ambiguous out of context, such as category being a recipe category on a recipe and a resource category on an ore, it reports only when the name fits none of them
    - The same script skips commented-out lines, which were being reported as if the game read them
    - Its technology check now covers add_unlock, add_prereq, remove_prereq and the rest of the helpers, not only TECHNOLOGY(). That is what surfaced bob-rtg. Which of a technology or a recipe the name has to be depends on the receiver, so it asks only whether upstream mentions the name at all, which is enough to catch a rename
    - check-optional-deps.ps1 follows require() and counts a guard in a parent file, since most override files are pulled in by a parent that has already tested for the mod. That removed fifteen false reports
    - It also reads ingredient and result names, because one naming a prototype that does not exist fails validation exactly like a bad category does. Every name we use turned out to resolve, so nothing needed changing there
---------------------------------------------------------------------------------------------------
Version: 2.1.13
Date: 2026-08-11
  Bugfixes:
    - Fix the load failure on bob-heat-shield-tile: Angel's prefixed its recipe categories, so the smelting override has to ask for angels-sintering rather than sintering. A bad category is only caught when prototypes are validated, which is why it surfaced against a recipe rather than at the line that set it
    - The seaweed-to-coalgas recipe we add is in angels-bio-processing for the same reason; under the old name it was rejected the moment angelsbioprocessing was active
    - Bring the distillery exclusivity override up to Bob's 2.x, where the category is bob-distillery and the distilleries are furnaces rather than assembling machines, so it stops looking for machines that cannot match
  Modding:
    - check-prototype-names.ps1 gained a category check. Recipe categories are prototypes too, and nothing until now verified that one we assign exists. It reads the plural categories field, replace_category swaps, and categories pushed onto a machine after the fact
    - It also recognises prototypes declared with type and name on a single line, which is how Angel's writes its recipe categories
---------------------------------------------------------------------------------------------------
Version: 2.1.12
Date: 2026-08-11
  Bugfixes:
    - Fix the load crash on data.raw.recipe['nitrogen']. The name was right but the recipe is pyrawores', not bobplates', so the override ran in games that have no nitrogen recipe at all
    - Guard every other prototype we touch whose owning mod the surrounding condition does not actually guarantee, so a partial mod set no longer aborts the load: the twelve prereq swaps in the no-angels electronics block reach into bobmining, bobpower, bobtech, bobassembly, bobequipment, bobvehicleequipment and boblogistics; the drill technologies under boblogistics are bobmining's; laika, crusher-2 and electric-mining-drill are pyalienlife's; py-burner is pyindustry's; water treatment and the clarifier are angelsrefining's, not angelspetrochem's
    - Only apply the belt-overhaul prereq when boblogistics is present, since both the setting it reads and the logistics-0 technology come from that mod
    - The pyhardmode void-machine conversion no longer assumes the clarifier exists; without angelsrefining it was turning a missing furnace into an assembling machine
    - Rewrite the prod-for-sinners module pass as a loop that skips modules that are not installed. The setting says nothing about whether pyalienlife or pyalternativeenergy is on, so all 84 of those lines could crash
    - Bob's 2.0 renamed its ore resources: the mining fluid overrides now use bob-zinc-ore, bob-nickel-ore and bob-tin-ore, and the hidden duplicate plate recipe is bob-silver-plate
  Modding:
    - Add scripts/check-optional-deps.ps1, which finds the class of bug above by resolving which mod defines each prototype we touch and asking whether reaching that line requires the mod to be installed. It splits results by consequence, because a missing TECHNOLOGY() or FLUID() aborts the load while a missing RECIPE() only skips the override
    - check-prototype-names.ps1 now asks whether a prototype of the right type exists rather than whether any mod mentions the name, which is what let recipe 'nitrogen' pass while a fluid of that name existed. It no longer skips single-word names either
---------------------------------------------------------------------------------------------------
Version: 2.1.11
Date: 2026-08-10
  Bugfixes:
    - Sweep every prototype name we reference against the mods in othermodsource and repoint the ones renamed upstream, instead of finding them one crash at a time
    - Fix two more load crashes: angels-rocket-fuel-capsule / angels-rocket-oxidizer-capsule were injected into the rocket-fuel recipe under their old unprefixed names, and angels-crystal-grindstone into eg-si, so prototype validation rejected an unknown ingredient
    - Bob's 3.0 prefixes we had missed: the whole bobclasses player-* body part line, exoskeleton-equipment-2, steel-bearing-ball, electrolysis-1, silicon-nitride, synthetic-wood, sour-gas, ammoniated-brine, ferric-chloride-solution, chemical-plant-2/3/4, gold/silver/rutile ore, and bob-nickel-processing (previously misspelt "nickel-procesing")
    - Angel's 2.1 prefixes we had missed: powder-metallurgy-1/4, algae-farm and algae-farm-2, water-washing-1, puffer-butchery-1, crystal-grindstone, algae-green and algae-brown, so the seaweed-to-coalgas recipe is registered again
    - Angel's names that were reordered rather than just prefixed: cellulose is angels-cellulose-fiber, black liquor is angels-liquid-black-liquor, residual gas is angels-gas-residual, the petri dish is angels-petri-dish, and pressured air is angels-gas-compressed-air, so those merges into the Py equivalents happen again
    - Names that are Py's, not Angel's, and should never have carried an angels- prefix: melamine, melamine-resin, filtration, empty-planter-box, lead-plate-1
    - Angel's-ore-to-Py-ore merges pointed at item names Py does not use; they are ore-aluminium, ore-chromium, ore-lead, ore-nickel, ore-tin and ore-zinc
    - Bob's aluminium resource is bob-bauxite-ore and yields Py's ore-aluminium
    - Bob's advanced logistic science pack is bob-advanced-logistic-science-pack, so removing it from Angel's silo and warehouse technologies works
    - Fix the ralesia productivity modules (were "ralesias-mk03"/"-mk04"), which would have crashed data.lua for anyone with the prod-for-sinners setting on
    - Merge the first-tier centrifuge into centrifuge-mk01. Bob's only prefixes tier 2 and up, so tier 1 is the vanilla centrifuge and was never matched, while the mk02 and mk03 merges on the next lines already worked
    - Hide Py's tin-plate-1 when Angel's tin ore is enabled, which is what the block guarded by that setting always meant to do
  Changes:
    - Drop the auto_barrel workaround for xyhiphoe blood; PyAL renamed it to blood-xyhiphoe and turned it into an item, so there is no fluid left to fix
  Modding:
    - Add scripts/suggest-renames.ps1, which proposes what an unknown name was renamed to and names the mod and prototype type that defines each candidate
    - Add scripts/check-lua-strings.py, a structural check that a bulk edit over quoted names left every string and bracket balanced
---------------------------------------------------------------------------------------------------
Version: 2.1.10
Date: 2026-08-10
  Bugfixes:
    - Fix load crash on bob-turbo-underground-belt: Bob's 2.1 dropped the bob- prefix from the turbo belt tier, so turbo-underground-belt / turbo-transport-belt are the names now (ultimate keeps its prefix)
    - Fix load crash on unknown technologies: logistics-0 and energy-3 (not bob-logistics-0 / bob-energy-3), angels-solder-smelting-1 (not -basic), and the bob- prefixes the 2.1 pass missed in the no-angels overrides (advanced-research, electric-substation-2, exoskeleton-equipment-2, fluid-generator-2, invar-processing, lead-processing, personal-roboport-modular-equipment-1, vehicle-laser-defense-equipment-2, vehicle-roboport-modular-equipment-1, vehicle-shield-equipment-1)
    - Stop injecting the non-existent bob-robot-brain-construction item into py-construction-robot-mk01; Bob's item is bob-robot-brain
    - Angel's resin technology is angels-resin-1, so the prereq and science-pack overrides for it apply again instead of being skipped
  Modding:
    - Add scripts/check-prototype-names.ps1, which reports prototype names we reference that no longer exist in othermodsource, so renames turn up in one pass instead of one crash at a time
---------------------------------------------------------------------------------------------------
Version: 2.1.9
Date: 2026-08-10
  Bugfixes:
    - PyCoalTBaA stub 0.0.7 now stamps recipe:has_category itself in data-updates and no longer depends on AdminUnknownFixes being present or loading first; every mod's data-updates runs before every mod's data-final-fixes, so this lands ahead of pycoalprocessing's checks whatever the mod order turns out to be
    - Stamp has_category as a callable table instead of a function, so it survives serialisation on its own ("Cannot serialise ttype=function") rather than depending on a later cleanup pass
    - Both mods log recipe counts under [AdminUnknownFixes] and [PyCoalTBaA stub] so factorio-current.log shows which stages actually ran
---------------------------------------------------------------------------------------------------
Version: 2.1.8
Date: 2026-08-10
  Bugfixes:
    - Fix pycoalprocessing data-final-fixes crash (recipe:has_category nil) by stamping the helper onto every recipe as a plain field at the end of data-updates, which survives any mod that replaces a prototype's metatable in an earlier data-final-fixes; the stamps are removed again at the end of our own data-final-fixes so Factorio can still serialise the prototypes
    - Recipes added after that point pick the helpers up through a data.raw.recipe __newindex hook, so data:extend during final-fixes is covered too
    - Log which prototypes lost their pypostprocessing metatable and which mod stage restored them, so the culprit is named in factorio-current.log
---------------------------------------------------------------------------------------------------
Version: 2.1.7
Date: 2026-08-10
  Bugfixes:
    - Fix pycoalprocessing data-final-fixes crash (recipe:has_category nil) without depending on load order: data.raw.recipe now carries a __pairs hook that rebinds pypostprocessing's recipe helpers inside the caller's own loop, so any prototype that lost its metatable is healed before pycoalprocessing reads it
    - Recipe helpers fall back to a self-contained has_category when pypostprocessing's table cannot be found, and forward every other method to pypostprocessing, so the heal can no longer silently do nothing
    - Drop the global pairs() override, which bypassed __pairs metamethods for every mod loaded after us
    - PyCoalTBaA stub 0.0.6: share the same guard instead of its own rebind, and never abort the load
---------------------------------------------------------------------------------------------------
Version: 2.1.6
Date: 2026-08-10
  Bugfixes:
    - PyCoalTBaA stub 0.0.5: drop graphics peer-dep (case-insensitive sort put stub after pycoal so heal never ran); rebind all recipes via RECIPE() in an earlier DFF wave; fail loudly if has_category still missing
---------------------------------------------------------------------------------------------------
Version: 2.1.5
Date: 2026-08-10
  Bugfixes:
    - PyCoalTBaA stub 0.0.4: rebind every recipe via RECIPE() in DFF; depend on pycoalprocessinggraphics so heal runs in pycoal's load cohort (not too early); hard-error if any recipe still lacks has_category
---------------------------------------------------------------------------------------------------
Version: 2.1.4
Date: 2026-08-10
  Bugfixes:
    - PyCoalTBaA stub 0.0.3: restore has_category via setmetatable only (own-field functions are not serialisable); depend on pypostprocessing; strip illegal function fields from older AUF heals
---------------------------------------------------------------------------------------------------
Version: 2.1.3
Date: 2026-08-10
  Bugfixes:
    - Move recipe has_category heal into PyCoalTBaA stub 0.0.2 data-final-fixes (loads before pycoal); AUF alone is always too late when optional Py deps are enabled
---------------------------------------------------------------------------------------------------
Version: 2.1.2
Date: 2026-08-10
  Bugfixes:
    - Fix pycoalprocessing has_category nil for real: require PyPP lib, stamp own-field has_category on every recipe, re-heal at end of data-updates (pairs wrap cannot reach other mods' sandboxes)
---------------------------------------------------------------------------------------------------
Version: 2.1.1
Date: 2026-08-10
  Bugfixes:
    - Fix pycoalprocessing data-final-fixes crash (recipe:has_category nil): cache PyPP recipe metatables early and re-heal on pairs(data.raw.recipe) so stripped metas are restored before Py's tar/combustion checks
---------------------------------------------------------------------------------------------------
Version: 2.1.0
Date: 2026-08-10
  Changes:
    - Factorio 2.1: factorio_version 2.1, base/Py/Angels/Bob dependency floors, recipe category → categories API, omni-science-pack as item
    - Require pypostprocessing >= 3.1.2; remove pyppatba stub, packaging, and install docs (no longer needed)
    - Bump PyCoalTBaA stub to factorio_version 2.1 (still required for Angels + Py gate)
    - Remove obsolete 2.0-era overrides; fix Bob bob- prefixes, electronics prereq, washer angels-washing-plant merge, urea fluid/unlock names
---------------------------------------------------------------------------------------------------
Version: 2.0.48
Date: 2026-04-23
  Changes:
    - Commit stub release zips under stubs/ with raw GitHub download URLs in info.json and new README; package-mods copies stub zips into stubs/ after each build; add docs/pypostprocessing-upstream-pr.md for proposed PyPP PR (AdminUnknownFixes + pyppatba dependency)
---------------------------------------------------------------------------------------------------
Version: 2.0.47
Date: 2026-04-23
  Changes:
    - PyHighTech + bobelectronics icon merge: intelligent-unit uses 32x32 asset; set icon_size 32 (fixes 64x64 sprite rectangle error on intelligent-unit.png)
---------------------------------------------------------------------------------------------------
Version: 2.0.46
Date: 2026-04-23
  Changes:
    - Bob's lab 2 + Py: merge research packs from any technology using bob-science-pack-gold and bob-alien-science-pack* into bob-lab-2.inputs (fixes no lab accepts bob-acid-bullets packs)
---------------------------------------------------------------------------------------------------
Version: 2.0.45
Date: 2026-04-23
  Changes:
    - Angel's petrochem + pyhightech: guard angels-mining-with-fluid TECHNOLOGY() and tech_remove_recipe when that technology is absent (Angel's 2.0 / mod options)
---------------------------------------------------------------------------------------------------
Version: 2.0.44
Date: 2026-04-22
  Changes:
    - urea-gasification: remove main_product gas-urea (PyPP ITEM() treats main_product as item; crashes). Only require urea recipe when gas-urea fluid and urea item exist
---------------------------------------------------------------------------------------------------
Version: 2.0.43
Date: 2026-04-22
  Changes:
    - Bob's enemies + bobtech + pyhightech: only touch bob-quantum when that technology exists; guard add_prereq(bob-alien-research) on bob-alien-research existing
---------------------------------------------------------------------------------------------------
Version: 2.0.42
Date: 2026-04-22
  Changes:
    - Angel's petrochem + pyhightech: guard angels-melamine TECHNOLOGY() and angels-melamine-resin RECIPE() when prototypes missing (PyPP errors on missing tech)
---------------------------------------------------------------------------------------------------
Version: 2.0.41
Date: 2026-04-22
  Changes:
    - Angel's + PyPP: only call TECHNOLOGY(vacuum-tube-electronics) when that prototype exists (removed/renamed in current pyhightech; fixes data-updates crash in overrides-updates / overrides-fixes)
---------------------------------------------------------------------------------------------------
Version: 2.0.40
Date: 2026-04-22
  Changes:
    - Post-replacer next_upgrade clear: include oil-refinery (same hidden-minable vs next_upgrade rule as chemical-plant)
---------------------------------------------------------------------------------------------------
Version: 2.0.39
Date: 2026-04-22
  Changes:
    - chemical-plant: clear next_upgrade after global_item_replacer (unconditional); earlier conditional ran before Py hid the mined item so the engine still rejected next_upgrade + hidden product
---------------------------------------------------------------------------------------------------
Version: 2.0.38
Date: 2026-04-22
  Changes:
    - chemical-plant: clear next_upgrade when minable returns a hidden item (Factorio 2.0 rule; Angel's path skipped the old no-angels-only fix)
---------------------------------------------------------------------------------------------------
Version: 2.0.37
Date: 2026-04-22
  Changes:
    - Bob's greenhouse: set allowed_effects to speed and consumption only instead of empty table (Factorio 2.0 rejects module slots with no allowed effects)
---------------------------------------------------------------------------------------------------
Version: 2.0.36
Date: 2026-04-22
  Changes:
    - Angel's bioprocessing + Py: set_to_py1_with_bio only adds token-bio research ingredient when that tool/item exists; otherwise same packs as set_to_py1 (fixes assignID when pyalienlife removes or omits token-bio)
---------------------------------------------------------------------------------------------------
Version: 2.0.35
Date: 2026-04-22
  Changes:
    - PyCoal removes oil-gathering: strip that prerequisite from every technology when absent (covers angels-gas-processing and any other Angel's techs, not only angels-oil-processing)
---------------------------------------------------------------------------------------------------
Version: 2.0.34
Date: 2026-04-22
  Changes:
    - Angel's Petrochem + PyCoal: drop oil-gathering from angels-oil-processing prerequisites when that tech was removed (fixes assignID)
---------------------------------------------------------------------------------------------------
Version: 2.0.33
Date: 2026-04-22
  Changes:
    - sand-processing / bob-quartz Angel's fix: do not use data.raw.entity in data stage (nil); only check data.raw.resource for mine-trigger targets
---------------------------------------------------------------------------------------------------
Version: 2.0.32
Date: 2026-04-22
  Changes:
    - Angel's Refining + Bob/AAI: when bob-quartz is removed, retarget sand-processing research_trigger from bob-quartz to ore-quartz/quartz if present, else craft-item sand (fixes assignID crash)
---------------------------------------------------------------------------------------------------
Version: 2.0.31
Date: 2026-04-22
  Changes:
    - Angel's refining + Py: only add merged crafting_categories when data.raw["recipe-category"][name] exists (Angels 2.0 dropped angels-washer in some builds; avoids assignID crash on washer assembling-machine)
---------------------------------------------------------------------------------------------------
Version: 2.0.30
Date: 2026-04-22
  Changes:
    - Angel's overrides: use Factorio 2.0 ResearchIngredient tuple form { "item", amount } for technology unit.ingredients (set_to_py1, alien artifact techs). Fixes prototype validation error on angels-ore-floatation etc. when PyPP runs after our data-final-fixes pass.
---------------------------------------------------------------------------------------------------
Version: 2.0.29
Date: 2026-04-22
  Changes:
    - Bob's modules compat: use Factorio 2.0 flat-number ModuleEffects (speed/consumption/productivity/pollution as numbers, not { bonus = ... }) so speed-module and other module prototypes validate
---------------------------------------------------------------------------------------------------
Version: 2.0.28
Date: 2026-04-22
  Changes:
    - Bob's modules compat: guard TECHNOLOGY() and bobmods.lib.tech tweaks when techs are absent (e.g. module-merging removed in current Bob's). Guard module effect and recipe ingredient overrides when prototypes are missing.
---------------------------------------------------------------------------------------------------
Version: 2.0.27
Date: 2026-04-22
  Changes:
    - Bob's plates: only adjust gas-canisters technology when it exists (PyPP TECHNOLOGY() errors if missing; tech removed/renamed in current Py + Bob combo)
---------------------------------------------------------------------------------------------------
Version: 2.0.26
Date: 2026-04-22
  Changes:
    - tech_merge / tech_merge_effects: no-op when either technology is missing or has no effects (Bob's Plates no longer defines fluid-canister-processing in some versions; avoids nil index in data-final-fixes)
---------------------------------------------------------------------------------------------------
Version: 2.0.25
Date: 2026-04-22
  Changes:
    - Bob's logistics: guard inserter filter_count overrides (steam-inserter etc. absent in current Bob's Logistics / Factorio 2.0)
---------------------------------------------------------------------------------------------------
Version: 2.0.24
Date: 2026-04-22
  Changes:
    - Angel's bioprocessing: guard alien-artifact colour technology unit.ingredient overrides (crashed when tech or unit absent in current angelsbioprocessing)
---------------------------------------------------------------------------------------------------
Version: 2.0.23
Date: 2026-04-22
  Changes:
    - pypp compatibility: broaden error() patch to suppress the full Pyanodon impossible-to-research / hidden-prerequisite diagnostic (not only sulfur-processing; fixes bob-centrifuge-2 / uranium-processing and similar stragglers). Renamed helper to patch-pypp-impossible-research-validation.lua
---------------------------------------------------------------------------------------------------
Version: 2.0.22
Date: 2026-04-22
  Changes:
    - Angel's + pypp: remap sulfur-processing to angels-sulfur-processing-1 in prerequisites for all technologies (not only explosives; fixes battery and any other stragglers). Broaden error() suppression to any impossible-to-research message mentioning hidden prerequisite sulfur-processing.
---------------------------------------------------------------------------------------------------
Version: 2.0.21
Date: 2026-04-22
  Changes:
    - Angel's + pypp: wrap global error() from end of data-updates until start of our data-final-fixes to suppress only pypostprocessing's impossible-to-research check for explosives + hidden sulfur-processing (Angel's data-final-fixes can re-hide sulfur after data-updates; load order vs pypp is not controllable from AdminUnknownFixes alone)
---------------------------------------------------------------------------------------------------
Version: 2.0.20
Date: 2026-04-22
  Changes:
    - Angel's Petrochem + pypp: fix explosives / sulfur-processing impossible-to-research error when Angel's second OV.execute in data-final-fixes hides sulfur after data-updates (temporarily clear hidden for pypp check; restore at start of our data-final-fixes). Also remove erroneous early return in sulfur-prereq remap when angelspetrochem is active.
---------------------------------------------------------------------------------------------------
Version: 2.0.19
Date: 2026-04-22
  Changes:
    - Angel's Petrochem + pypp: remap explosives technology prerequisite from hidden sulfur-processing to angels-sulfur-processing-1 at end of data-updates (fixes pypostprocessing impossible-to-research check in data-final-fixes.lua)
---------------------------------------------------------------------------------------------------
Version: 2.0.18
Date: 2026-04-22
  Changes:
    - Compatibility: wrap pypostprocessing TECHNOLOGY() for missing bob-thorium-processing and bob-deuterium-fuel-reprocessing (pypp compatibility/bobs.lua assumes they exist when uranium-processing is hidden; current Bob's may omit them)
---------------------------------------------------------------------------------------------------
Version: 2.0.17
Date: 2026-04-22
  Changes:
    - Added PyCoalTBaA-stub sibling mod (name PyCoalTBaA) so pypostprocessing no longer errors with the Please install PyCoal Touched By an Angel message when angelsrefining is loaded (pypp compatibility.lua gate; bridge content is in AdminUnknownFixes)
    - package-mods.ps1 / package-mods.sh now also build dist/PyCoalTBaA_0.0.1.zip alongside AdminUnknownFixes and pyppatba
---------------------------------------------------------------------------------------------------
Version: 2.0.16
Date: 2026-04-22
  Changes:
    - Bob's assembly: fix typo 'table.deep_copy' -> 'table.deepcopy' in burner-start energy_source override for bob-assembling-machine-4/5/6
---------------------------------------------------------------------------------------------------
Version: 2.0.15
Date: 2026-04-22
  Changes:
    - Bob's plates + pyalienlife: guard grinding and bob-fluid-barrel-processing technology overrides (TECHNOLOGY() errors on missing techs; also skip pack swap when bob-fluid-barrel-processing uses research_trigger)
---------------------------------------------------------------------------------------------------
Version: 2.0.14
Date: 2026-04-22
  Changes:
    - Bob's ores + pyrawores: guard bob-ore-aluminium resource minable.results override (crashed when resource absent in current Bob's Ores)
---------------------------------------------------------------------------------------------------
Version: 2.0.13
Date: 2026-04-22
  Changes:
    - Angel's bioprocessing + pyalienlife: guard puffer-butchery-1 recipe main_product override (crashed when recipe absent in current pyalienlife)
---------------------------------------------------------------------------------------------------
Version: 2.0.12
Date: 2026-04-22
  Changes:
    - Angel's petrochem + pyalienlife: guard resin-1 / angels-basic-chemistry-3 / angels-melamine technology prerequisite overrides (crashed in current Factorio 2.0 stack because TECHNOLOGY() errors on missing techs unlike RECIPE())
---------------------------------------------------------------------------------------------------
Version: 2.0.11
Date: 2026-04-22
  Changes:
    - Fix load hang introduced in 2.0.9/2.0.10: the for-loop in the repair helpers used pairs as if it were a stateful iterator (for _, v in pairs, t, nil) which made the loop call pairs(t, nil) repeatedly and never terminate. Helpers now iterate with next directly (for _, v in next, t, nil).
---------------------------------------------------------------------------------------------------
Version: 2.0.10
Date: 2026-04-22
  Changes:
    - Fix load hang introduced in 2.0.9: the pairs() wrapper's repair helpers called the wrapped pairs themselves, causing infinite recursion. Helpers now capture and use the original pairs via rawget(_G, "pairs").
---------------------------------------------------------------------------------------------------
Version: 2.0.9
Date: 2026-04-22
  Changes:
    - Compatibility: wrap global pairs() so any iteration of data.raw.recipe or data.raw.technology self-heals missing pypostprocessing metatables (fixes 'attempt to call method replace_ingredient (a nil value)' in pyalienlife/data-updates.lua line 186 and similar direct-iteration crashes in mods that load between pypostprocessing and AdminUnknownFixes)
---------------------------------------------------------------------------------------------------
Version: 2.0.8
Date: 2026-04-22
  Changes:
    - Compatibility: reapply pypostprocessing recipe/technology metatables at end of data.lua, and wrap py.global_item_replacer / py.global_fluid_replacer / py.global_prerequisite_replacer so they self-heal on every call (fixes 'attempt to call method replace_ingredient (a nil value)' in pypetroleumhandling/data-updates.lua when other mods add prototypes via direct data.raw assignment)
---------------------------------------------------------------------------------------------------
Version: 2.0.7
Date: 2026-04-22
  Changes:
    - Bob's plates + pyalienlife: guard xyhiphoe-blood fluid auto_barrel override (crashed when fluid absent in current pyalienlife)
---------------------------------------------------------------------------------------------------
Version: 2.0.6
Date: 2026-04-22
  Changes:
    - Bob's mining + pyalienlife: skip bob-steel-axe-2 science-pack swap when technology uses research_trigger (Factorio 2.0 Bob's Mining no longer has unit/packs on this tech)
---------------------------------------------------------------------------------------------------
Version: 2.0.5
Date: 2026-04-22
  Changes:
    - Bob's greenhouse + pyalienlife: guard wood-pellets / seedling fuel_category overrides (crashed when items absent in current Bob's Greenhouse)
    - Bob's greenhouse + pyalienlife: guard bob-greenhouse technology prerequisite override
---------------------------------------------------------------------------------------------------
Version: 2.0.4
Date: 2026-04-22
  Changes:
    - Bob's revamp: guard heat-shield-tile recipe override (crashed when recipe absent in Factorio 2.0 / current Bob's Revamp)
    - Bob's revamp: guard heat-shield technology prerequisite and science-pack overrides
---------------------------------------------------------------------------------------------------
Version: 2.0.3
Date: 2026-04-22
  Changes:
    - Bob's warfare: guard rocket-engine recipe override (crashed when recipe absent in Factorio 2.0 / current Bob's Warfare)
    - Bob's warfare: guard bob-rocket technology access and fix 'prerequisits' typo so prerequisite override actually applies
---------------------------------------------------------------------------------------------------
Version: 2.0.2
Date: 2026-04-22
  Changes:
    - PyPostProcessing: use full ingredient tables for RECIPE():add_ingredient() (bare strings crashed recipe.lua)
    - Bob's logistics / pyhightech: bob-repair-pack-2, py-construction-robot-mk01 recipe overrides
    - Angel's water void: filtration-media on saline / purified void recipes
    - MSP (pyalienlife + pyhightech): more-science-pack-25 intelligent-unit ingredient
---------------------------------------------------------------------------------------------------
Version: 2.0.1
Date: 2026-04-22
  Changes:
    - Py washers: use washer / washer-mk02..mk04 instead of washing-plant (modern Py)
    - Global item replacer: map angels-washing-plant-3/4 to washer-mk03/mk04
    - Angel's bio: angels-cellulose-fiber and angels-wood-bricks item names
    - coalgas-from-seaweed: only register when algae-green and algae-brown items exist
    - angels-tin-plate-1: guard when recipe absent (e.g. without Bob's Plates)
    - Bob's: bob-drills-2 / bob-area-drills-2, bob-steel-axe-2..6 technology names
    - Bob's electronics overrides: guard optional recipes (silicon wafer, rubber, boards)
    - Added pyppatba-stub sibling mod for PyPostProcessing pyppatba dependency
---------------------------------------------------------------------------------------------------
Version: 2.0.0
Date: 2026-03-30
  Changes:
    - Renamed mod to AdminUnknownFixes
    - Updated to Factorio 2.0
    - Merged PyPPTBaA (Pyanodons Post Processing Touched By an Angel) into this mod
    - Merged bobsmodules4py (Bob's Modules 4 Pyanodons) into this mod
    - Updated all Angel's mod references for 2.0 (angels- prefix on prototypes)
    - Updated all Bob's mod references for 2.0 (bob- prefix on prototypes)
    - Updated all Py mod references for 2.0 (pypostprocessing 3.x API)
    - Replaced stdlib dependency with pypostprocessing built-in library
    - Removed normal/expensive difficulty handling (removed in Factorio 2.0)
    - Updated recipe format for 2.0 (no more shorthand ingredients/results)
    - Updated module_specification to module_slots
    - Updated emissions_per_minute format
    - Updated global item replacer mappings for all 2.0 mod renames
    - Added ingredient deduplicator (from PyPPTBaA)
    - Added underground belt recipe scaling for Bob's logistics (from PyPPTBaA)
    - Added Bob's modules tech/recipe/effect integration (from bobsmodules4py)
---------------------------------------------------------------------------------------------------
